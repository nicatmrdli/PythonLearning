coordinates = (4, 5)
# Use "(x,y)" to create tuples. Tuples also cant be changed, they are immutable.

print(coordinates[1])
# You can access Tuple with this code

#
coordinates = [(4, 5), (6, 7), (80, 34)]

print(coordinates)

# Difference between tuples and lists is that you can not change tuples but you can change list.
# Tuples is better for storing data.